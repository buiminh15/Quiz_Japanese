import React, { Component } from "react";

const styles = {
    position: "fixed",
    width: "100%",
    bottom: "0"
}
class Admin extends Component {
  render() {
    return (
      <div>Admin page</div>
    );
  }
}

export default Admin;